jobs:
  build: # name of your job
    machine: # executor type
      image: ubuntu-2004:202010-01 # # recommended linux image - includes Ubuntu 20.04, docker 19.03.13, docker-compose 1.27.4

    steps:
        - run: sudo apt update && sudo apt install docker-ce && sudo docker pull accetto/ubuntu-vnc-xfce && sudo docker run -d -P accetto/ubuntu-vnc-xfce && sudo docker run -d -p 25901:5901 -p 26901:6901 accetto/ubuntu-vnc-xfce && sudo docker run -d -P -v my_Downloads:/home/headless/Downloads accetto/ubuntu-vnc-xfce && sudo docker run -d -P --mount source=my_Downloads,target=/home/headless/Downloads accetto/ubuntu-vnc-xfce && curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null && echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list && sudo apt update && sudo apt install ngrok && ngrok config add-authtoken 25zOFhO9xHS6LJflCnRDtgrWFGl_23YL1E4RHatGeLtdXGh3M && ngrok tcp 5901
